import { defineTools } from '../types.js';

/**
 * Emulation tools. Placeholder: the tools themselves land in a
 * dedicated stream. Composed into the registry by schemas.ts already, so
 * adding an entry here is the only edit a new emulation tool needs.
 */
export const emulationTools = defineTools({});
