import { defineTools } from '../types.js';

/**
 * Storage tools. Placeholder: the tools themselves land in a
 * dedicated stream. Composed into the registry by schemas.ts already, so
 * adding an entry here is the only edit a new storage tool needs.
 */
export const storageTools = defineTools({});
